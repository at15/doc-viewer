#!/usr/bin/env node

var path = require('path');
var server = require('../lib/server');
// TODO:set it to current working dir.
//server.setDocRoot();
//console.log(process.cwd());
server.setDocRoot(path.join(process.cwd(), 'doc'));
server.start();